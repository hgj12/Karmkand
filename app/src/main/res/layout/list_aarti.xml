<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_lg"
        android:layout_marginTop="@dimen/spacing_xs"
        android:layout_marginEnd="@dimen/spacing_lg"
        android:layout_marginBottom="@dimen/spacing_xs"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        app:cardBackgroundColor="@color/surface_card"
        app:cardCornerRadius="@dimen/card_corner_radius"
        app:cardElevation="2dp"
        app:contentPaddingBottom="@dimen/spacing_lg"
        app:contentPaddingLeft="@dimen/spacing_lg"
        app:contentPaddingRight="@dimen/spacing_lg"
        app:contentPaddingTop="@dimen/spacing_lg">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvDetail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/sanskrit"
            android:gravity="start"
            android:lineSpacingExtra="@dimen/reading_line_spacing"
            android:lineSpacingMultiplier="1.3"
            android:textColor="@color/white"
            android:textSize="@dimen/title_medium" />

    </androidx.cardview.widget.CardView>

</LinearLayout>